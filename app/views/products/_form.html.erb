<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, t('attributes.product.name') %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :quantity, t('attributes.product.quantity')  %><br />
    <%= f.number_field :quantity %>
  </div>
  <div class="field">
    <%= f.label :description, t('attributes.product.description')  %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
